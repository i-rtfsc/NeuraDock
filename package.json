{
  "name": "neuradock",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@9.15.5",
  "scripts": {
    "dev": "pnpm run dev:desktop",
    "dev:desktop": "pnpm -C apps/desktop run tauri:dev",
    "build": "pnpm run build:desktop",
    "build:desktop": "pnpm -C apps/desktop run tauri:build"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
